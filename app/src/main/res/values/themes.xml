<resources>
    <!-- Base application theme using Material Components instead of Material3 -->
    <style name="Theme.KaminaApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <!-- Set a simple solid color background for testing -->
        <item name="android:windowBackground">@color/background</item> <!-- Use black as a simple color -->
    </style>
</resources>
